# Build output
/target

# Generated HTML report (`tv report` / `--report`)
tv-report.html

# Cargo.lock is committed: this is a binary/CLI, so the lock is deliberately tracked.
# The blame-at-death cache lives inside .git (<gitdir>/tv-cache), so it is never in
# the working tree and needs no ignore rule.
